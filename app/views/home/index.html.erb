<h2>Tweets</h2>

<ul>
  <% @tweets.each do |tweet| %>
    <li><b><%= link_to tweet.user, tweet.user %></b> <%= tweet %></li>
  <% end %>
</ul>

<h2>Users</h2>

<ul>
  <% @users.each do |user| %>
    <li><b><%= link_to user, user %></b></li>
  <% end %>
</ul>
