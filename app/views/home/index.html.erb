<h2>All Tweets</h2>

<ul>
  <% @tweets.each do |tweet| %>
    <li><b><%= link_to tweet.user, tweet.user %></b> <%= tweet %></li>
  <% end %>
</ul>

<h2>All Users</h2>

<ul>
  <% @users.each do |user| %>
    <li><b><%= link_to user, user %></b></li>
  <% end %>
</ul>
